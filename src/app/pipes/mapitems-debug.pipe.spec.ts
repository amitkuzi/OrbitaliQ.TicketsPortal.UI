import { MapitemsDebugPipe } from './mapitems-debug.pipe';

describe('MapitemsDebugPipe', () => {
  it('create an instance', () => {
    const pipe = new MapitemsDebugPipe();
    expect(pipe).toBeTruthy();
  });
});
